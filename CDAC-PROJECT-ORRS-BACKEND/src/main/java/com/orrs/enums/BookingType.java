package com.orrs.enums;


public enum BookingType {   
    INDIVIDUAL,         // Standard booking (Up to 6 passengers usually)   
    GROUP,              // Bulk booking (e.g., > 10 pax, might need Admin Approval)   
}
