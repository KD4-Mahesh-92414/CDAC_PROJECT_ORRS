package com.orrs.enums;

public enum StopType {
	 REGULAR,    // Passenger can board/alight
     TECHNICAL,  // Only for operational needs (no booking allowed)
     TERMINUS    // Start or End of the line
}
